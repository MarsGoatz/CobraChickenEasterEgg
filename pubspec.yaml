name: react_van
description: A project for React Van with easter eggs.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  mobx: ^2.0.0
  flutter_mobx: ^2.0.0
  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.12.2
  mobx_codegen: ^2.0.0
  gesture_x_detector: ^1.0.0  

flutter:
  uses-material-design: true

  assets:
    - cobrachicken.jpg
    - duck.jpg
    - gryffindor.jpg
    - slytherin.jpg
